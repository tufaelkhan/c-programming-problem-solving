#include<stdio.h>

int main(){
    int a, b;
    // char p;
    // scanf("%d%c %d%c", &a,&p, &b,&p);
    // printf("%d%% \n%d%%", a, b);

    scanf("%d%% %d%%", &a, &b);
    printf("%d%% \n%d%%", a, b);
    return 0;
}